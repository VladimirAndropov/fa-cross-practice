import React from 'react';
import { StyleSheet, Text, View, Image, TouchableOpacity } from 'react-native';

const BrightJournal = () => {
    const handleReadMore = () => {
        // Add your navigation or action for "Read More" here
        console.log('Читать далее clicked');
    };

    return (
        <View style={styles.container}>
            {/* Header */}
            <Text style={styles.header}>Журнал Bright</Text>

            {/* News Section */}
            <Text style={styles.newsSection}>Новости</Text>

            {/* Main Content Block */}
            <View style={styles.contentBlock}>
                <Image 
                    source={{ uri: 'https://nobelvape.ru/upload/iblock/a72/a723b1954f58a8704e8975338a9b55e9.jpg' }} 
                    style={styles.image} 
                />
                <Text style={styles.title}>Превращаем стресс в своего помощника</Text>
                <Text style={styles.description}>
                    Исследователи Йельского университета заявляют, что люди, которые рассматривают стресс, как возможность личностного роста, отмечают улучшение качества жизни. Сегодня узнаем, как это работает и как увидеть положительные стороны стресса.
                </Text>

                {/* Read More Button */}
                <TouchableOpacity style={styles.button} onPress={handleReadMore}>
                    <Text style={styles.buttonText}>Читать далее</Text>
                </TouchableOpacity>
            </View>
        </View>
    );
};

const styles = StyleSheet.create({
    container: {
        flex: 1,
        padding: 16,
        backgroundColor: '#F5F5F5',
    },
    header: {
        fontSize: 24,
        fontWeight: 'bold',
        textAlign: 'center',
        marginBottom: 20,
    },
    newsSection: {
        color: 'blue',
        fontSize: 18,
        marginBottom: 10,
    },
    contentBlock: {
        backgroundColor: 'white',
        borderRadius: 8,
        padding: 16,
        shadowColor: '#000',
        shadowOffset: { width: 0, height: 2 },
        shadowOpacity: 0.1,
        shadowRadius: 4,
        elevation: 2,
    },
    image: {
        width: '100%',
        height: 150,
        borderRadius: 8,
        marginBottom: 16,
    },
    title: {
        fontSize: 20,
        fontWeight: 'bold',
        marginBottom: 10,
    },
    description: {
        fontSize: 16,
        color: '#666',
        marginBottom: 20,
    },
    button: {
        backgroundColor: '#007BFF',
        paddingVertical: 10,
        borderRadius: 5,
        alignItems: 'center',
    },
    buttonText: {
        color: '#fff',
        fontSize: 16,
        fontWeight: 'bold',
    },
});

export default BrightJournal;
